# ==================== 时区设置 ====================
# 系统时区
TZ=Asia/Shanghai
# n8n 时区
GENERIC_TIMEZONE=Asia/Shanghai

# ==================== 安全配置 ====================
# 加密密钥
N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY:-your-encryption-key-here}

# ==================== 日志配置 ====================
# 日志级别
N8N_LOG_LEVEL=info
# 日志输出定向
N8N_LOG_OUTPUT=console

# ==================== 执行记录配置 ====================
# 是否保存成功执行的记录 (all, none)
EXECUTIONS_DATA_SAVE_ON_SUCCESS=all
# 是否保存失败执行的记录 (all, none)
EXECUTIONS_DATA_SAVE_ON_ERROR=all
# 是否自动清理旧的执行记录
EXECUTIONS_DATA_PRUNE=true
# 执行记录保留时间 (小时)，默认 336 = 14 天
EXECUTIONS_DATA_MAX_AGE=336

# ==================== 访问配置 (optional) ====================
# 如果通过反向代理或域名访问 n8n，请配置以下变量
# Webhook 回调地址 (必须是外部可访问的完整 URL)
# WEBHOOK_URL=https://n8n.your-domain.com/
# n8n 主机名
# N8N_HOST=n8n.your-domain.com

# =============== PostgreSQL 数据库配置 (optional) ==================
# 如果需要使用 PostgreSQL 替代默认的 SQLite，请配置以下变量
# 并在 compose.yaml 中取消相关注释
DB_POSTGRESDB_DATABASE=${DB_POSTGRESDB_DATABASE:-n8n}
DB_POSTGRESDB_USER=${DB_POSTGRESDB_USER:-n8n}
DB_POSTGRESDB_PASSWORD=${DB_POSTGRESDB_PASSWORD:-n8n}
