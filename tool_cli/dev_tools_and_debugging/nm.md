# nm

> 列出目标文件中的符号。
> 更多信息：<https://manned.org/nm>

- 查看符号表：
  `nm {{文件名}}`

- 显示未定义的符号：
  `nm -u {{文件名}}`

- 显示所有符号（包括调试符号）：
  `nm -a {{文件名}}`

- 按地址排序：
  `nm -n {{文件名}}`

- 显示符号大小：
  `nm -S {{文件名}}`