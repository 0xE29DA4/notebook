# file

> 判断文件类型。
> 更多信息：<https://manned.org/file>

- 判断文件类型：
  `file {{文件}}`

- 显示 MIME 类型：
  `file --mime-type {{文件}}`

- 显示详细的 MIME 信息：
  `file --mime {{文件}}`

- 不跟随符号链接：
  `file -h {{符号链接}}`

- 批量判断文件类型：
  `file {{文件1}} {{文件2}}`

- 从文件列表中读取文件名：
  `file -f {{文件列表}}`