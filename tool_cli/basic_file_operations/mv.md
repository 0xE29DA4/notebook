# mv

> 移动或重命名文件和目录。
> 更多信息：<https://manned.org/mv>

- 将文件或目录移动到新位置：
  `mv {{路径/到/源}} {{路径/到/目标}}`

- 重命名文件或目录：
  `mv {{旧名称}} {{新名称}}`

- 移动文件到另一个目录，保留原文件名：
  `mv {{路径/到/源文件}} {{路径/到/目标目录}}`

- 在覆盖现有文件前进行提示：
  `mv -i {{路径/到/源文件}} {{目标}}`

- 不要覆盖目标位置已存在的同名文件：
  `mv -n {{路径/到/源文件}} {{目标}}`
