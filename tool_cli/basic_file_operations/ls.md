# ls

> 列出目录内容。
> 更多信息：<https://manned.org/ls>

- 每行显示一个文件：
  `ls -1`

- 列出所有文件，包括隐藏文件：
  `ls -a`

- 列出所有文件，并在目录名后加上 `/`：
  `ls -F`

- 以长格式列出（显示权限、所有者、大小和修改时间）：
  `ls -l`

- 列出文件并以人类可读的单位（KB, MB, GB）显示大小：
  `ls -lh`

- 按大小降序排列：
  `ls -lS`

- 按修改时间按从旧到新排列：
  `ls -ltr`
