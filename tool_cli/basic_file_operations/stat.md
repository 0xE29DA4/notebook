# stat

> 显示文件或文件系统的状态信息。
> 更多信息：<https://manned.org/stat>

- 显示文件状态：
  `stat {{文件}}`

- 显示文件系统状态而非文件状态：
  `stat -f {{文件}}`

- 以简洁格式显示：
  `stat -t {{文件}}`

- 使用指定格式显示：
  `stat -c "%n: %s bytes" {{文件}}`

- 显示常用格式（权限、大小、修改时间）：
  `stat -c "%A %s %y %n" {{文件}}`

- 显示文件的 inode 号：
  `stat -c "%i" {{文件}}`