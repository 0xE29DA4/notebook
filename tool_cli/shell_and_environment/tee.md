# tee

> 从标准输入读取，写入文件和标准输出。
> 更多信息：<https://manned.org/tee>

- 将标准输入写入文件并同时输出到屏幕：
  `{{命令}} | tee {{文件}}`

- 追加到文件而非覆盖：
  `{{命令}} | tee -a {{文件}}`

- 写入多个文件：
  `{{命令}} | tee {{文件1}} {{文件2}}`

- 忽略中断信号：
  `{{命令}} | tee -i {{文件}}`

- 配合 sudo 写入需要 root 权限的文件：
  `echo "内容" | sudo tee {{/etc/配置文件}}`