# bzip2

> 压缩或解压文件（.bz2），压缩率通常比 gzip 高。
> 更多信息：<https://manned.org/bzip2>

- 压缩文件：
  `bzip2 {{文件名}}`

- 解压文件：
  `bzip2 -d {{文件名.bz2}}`

- 压缩文件并保留原文件：
  `bzip2 -k {{文件名}}`

- 设置压缩级别（1-9）：
  `bzip2 -{{级别}} {{文件名}}`

- 测试压缩文件完整性：
  `bzip2 -t {{文件名.bz2}}`