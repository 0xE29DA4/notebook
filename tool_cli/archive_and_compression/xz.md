# xz

> 压缩或解压文件（.xz），高压缩率。
> 更多信息：<https://manned.org/xz>

- 压缩文件：
  `xz {{文件名}}`

- 解压文件：
  `xz -d {{文件名.xz}}`

- 压缩文件并保留原文件：
  `xz -k {{文件名}}`

- 设置压缩级别（1-9，默认 6）：
  `xz -{{级别}} {{文件名}}`

- 强制解压：
  `xz -df {{文件名.xz}}`
