# wc

> 统计文件的行数、字数和字节数。
> 更多信息：<https://manned.org/wc>

- 统计行数：
  `wc -l {{文件名}}`

- 统计所有信息（行数、字数、字节数）：
  `wc {{文件名}}`

- 只统计字数：
  `wc -w {{文件名}}`

- 只统计字节数：
  `wc -c {{文件名}}`

- 统计多个文件：
  `wc {{文件1}} {{文件2}}`