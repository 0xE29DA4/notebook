# patch

> 将补丁文件应用到原始文件。
> 更多信息：<https://manned.org/patch>

- 应用补丁：
  `patch {{文件名}} < {{补丁文件}}`

- 撤销补丁：
  `patch -R < {{补丁文件}}`

- 应用补丁到指定目录：
  `patch -d {{目录}} < {{补丁文件}}`

- 创建备份后应用补丁：
  `patch -b {{文件名}} < {{补丁文件}}`