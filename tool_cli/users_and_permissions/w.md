# w

> 显示已登录用户及其活动。
> 更多信息：<https://manned.org/w>

- 显示所有已登录用户及其活动：
  `w`

- 显示指定用户的活动：
  `w {{用户名}}`

- 不显示标题行：
  `w -h`

- 显示简短格式（不显示登录时间、JCPU、PCPU）：
  `w -s`

- 显示旧格式输出（不显示空闲时间）：
  `w -o`

- 显示远程主机信息（FROM 字段显示主机名而非 IP）：
  `w -f`