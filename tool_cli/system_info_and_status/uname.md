# uname

> 打印系统信息。
> 更多信息：<https://manned.org/uname>

- 打印所有信息（内核名称、主机名、内核版本等）：
  `uname -a`

- 打印更多信息
  `uname -v`

- 只打印内核版本：
  `uname -r`

- 只打印主机名：
  `uname -n`

- 只打印内核名称：
  `uname -s`

- 打印硬件架构：
  `uname -m`
