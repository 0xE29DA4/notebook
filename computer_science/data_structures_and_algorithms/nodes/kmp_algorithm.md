# KMP Algorithm

- 概念
  - 串的前缀：从第一个元素开始的子串
  - 串的后缀：以最后一个元素结束的子串
  - 串的真前缀：在所有串的前缀中剔除了原串
  - 串的真后缀：在所有串的前缀中剔除了原串
  - next 数组
    - 含义：当模式串在第 n 个元素匹配失败时，主串指针应该不变（除非第一个元素就匹配失败），而模式串指针应该指向第 next[n] 个元素
- next 数组
  - 求解方法：next[i] = 第 i 个字符前的子串的最长公共真前后缀 + 1
  - 优化：将 next 数组改造成 nextval 数组
