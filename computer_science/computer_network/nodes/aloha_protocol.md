# Aloha Protocol

## 纯 ALOHA

- 發送端数据立即打到介质上，然后等待 ACK
- 如果接收端收到了完整无误的数据，它会向发送端返回 ACK
- 发送端会设计一个超时计时器，如果冲突发生，设备会等待一段随机时间后再次尝试发送

## 时隙 ALOHA

- 将时间划分成固定的时隙
- 每个时隙的长度恰好等于一个数据帧的传输时间
- 用户只能在时隙的开始时刻发送数据帧
- 相比纯 ALOHA 降低了冲突概率
