# which

> 查找可执行文件的位置，显示命令的完整路径。
> 更多信息：<https://manned.org/which>

- 查找命令的完整路径：
  `which {{命令}}`

- 查找多个命令的路径：
  `which {{命令1}} {{命令2}}`

- 显示所有匹配的路径（不只是第一个）：
  `which -a {{命令}}`

- 查找命令并显示其手册页位置：
  `which {{命令}} && man {{命令}}`
