# head

> 输出文件的开头部分。
> 更多信息：<https://manned.org/head>

- 显示文件前 10 行（默认）：
  `head {{文件}}`

- 显示文件前 N 行：
  `head -n {{N}} {{文件}}`

- 显示文件前 N 个字节：
  `head -c {{N}} {{文件}}`

- 显示多个文件的开头：
  `head {{文件1}} {{文件2}}`

- 显示文件开头，不显示文件名标题：
  `head -q {{文件1}} {{文件2}}`
