# xz

> 压缩或解压文件（.xz），高压缩率。
> 更多信息：<https://manned.org/xz>

- 压缩文件：
  `xz paht/to/file`

- 解压文件：
  `xz -d path/to/file.xz`

- 压缩文件并保留原文件：
  `xz -k paht/to/file`

- 设置压缩级别（1-9，默认 6）：
  `xz [-number] paht/to/file`

- 强制解压：
  `xz -df path/to/file.xz`
