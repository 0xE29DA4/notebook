# rm

> 删除文件或目录。
> 更多信息：<https://manned.org/rm>

- 从指定路径删除文件：
  `rm {{路径/到/文件1}} {{路径/到/文件2}}`

- 递归删除目录及其所有子目录和文件：
  `rm -r {{路径/到/目录}}`

- 强制删除，不提示确认且忽略不存在的文件：
  `rm -f {{路径/到/文件}}`

- 交互式删除，每次删除前进行提示：
  `rm -i {{路径/到/文件}}`

- 显示已删除文件的名称：
  `rm -v {{路径/到/目录}}/*`
