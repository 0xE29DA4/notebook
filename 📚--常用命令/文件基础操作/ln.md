# ln

> 创建对文件或目录的链接（硬链接或符号链接）。
> 更多信息：<https://manned.org/ln>

- 创建指向文件或目录的符号链接（软链接）：
  `ln -s {{路径/到/目标}} {{路径/到/链接名}}`

- 创建指向文件的硬链接：
  `ln {{路径/到/目标文件}} {{路径/到/链接名}}`

- 覆盖已存在的同名链接：
  `ln -sf {{路径/到/新目标}} {{路径/到/链接名}}`

- 交互式操作，在覆盖前进行提示：
  `ln -si {{路径/到/目标}} {{路径/到/链接名}}`
