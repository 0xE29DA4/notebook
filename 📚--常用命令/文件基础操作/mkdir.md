# mkdir

> 创建目录。
> 更多信息：<https://manned.org/mkdir>

- 在当前目录下创建一个目录：
  `mkdir {{目录名}}`

- 递归创建目录（即使父目录不存在也会一并创建）：
  `mkdir -p {{路径/到/目录}}`

- 创建目录并设置其权限（类似 chmod）：
  `mkdir -m {{权限模式}} {{目录名}}`

- 显示创建每个目录后的详细信息：
  `mkdir -v {{目录名}}`
