# tree

> 以树状图列出目录内容。
> 更多信息：<https://manned.org/tree>

- 以树状图显示当前目录的文件和子目录：
  `tree`

- 显示隐藏文件：
  `tree -a`

- 仅显示目录：
  `tree -d`

- 设置显示的最大层级深度：
  `tree -L {{层级数}}`

- 显示完整路径前缀：
  `tree -f`

- 列出包含权限、所有者和修改时间的详细信息：
  `tree -p -u -g -D`

- 仅列出匹配特定模式的文件（支持通配符）：
  `tree -P '{{*.txt}}'`
