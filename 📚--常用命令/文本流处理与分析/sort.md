# sort

> 对文本文件的行进行排序。

- 默认排序 (字典序)
  ```shell
  sort file.txt
  ```

- 按数值排序
  ```shell
  sort -n file.txt
  ```

- 逆序排序
  ```shell
  sort -r file.txt
  ```

- 去重排序
  ```shell
  sort -u file.txt
  ```
