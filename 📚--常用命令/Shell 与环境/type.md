# type

> 显示命令的类型 (alias, keyword, function, builtin, file)。

- 查看 ls 是什么
  ```shell
  type ls
  # ls is aliased to `ls --color=auto`
  ```

- 查看 cd 是什么
  ```shell
  type cd
  # cd is a shell builtin
  ```
