# seq

> 打印数字序列。
> 更多信息：<https://manned.org/seq>

- 生成 1 到 N 的序列：
  `seq {{N}}`

- 指定范围：
  `seq {{起始}} {{结束}}`

- 指定范围和步长：
  `seq {{起始}} {{步长}} {{结束}}`

- 指定分隔符：
  `seq -s "{{分隔符}}" {{结束}}`

- 使用 printf 格式化：
  `seq -f "{{格式}}" {{结束}}`