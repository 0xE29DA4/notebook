# shift

> 移动位置参数 ($1, $2, ...)。常用于脚本中处理参数。

- 移动参数示例脚本
  ```shell
  #!/bin/bash

  echo "原始参数: $1 $2 $3"

  shift  # 默认移动 1 位，$2 变成 $1，以此类推

  echo "移动后: $1 $2 $3"
  ```

- 移动 N 位
  ```shell
  shift 2  # 向左移动 2 位
  ```
