# export

> 设置环境变量，使其在子 Shell 中也有效。
> 更多信息：<https://manned.org/export`

- 设置环境变量：
  `export {{变量名}}={{值}}`

- 添加到 PATH：
  `export PATH=$PATH:{{/新/路径}}`

- 查看所有环境变量：
  `export`

- 删除环境变量：
  `export -n {{变量名}}`
