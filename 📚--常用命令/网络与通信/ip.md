# ip

> 显示或操作路由、设备、策略路由和隧道 (新一代网络配置工具)。

- 显示 IP 地址
  ```shell
  ip addr show
  ```

- 启用/禁用接口
  ```shell
  sudo ip link set eth0 up
  sudo ip link set eth0 down
  ```

- 显示路由表
  ```shell
  ip route show
  ```
