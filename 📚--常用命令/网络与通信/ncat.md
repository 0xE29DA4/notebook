# ncat

> Netcat，网络工具中的瑞士军刀。
> 更多信息：<https://manned.org/ncat>

- 监听端口（作为服务器）：
  `nc -l {{端口}}`

- 连接端口（作为客户端）：
  `nc {{主机}} {{端口}}`

- 端口扫描：
  `nc -zv {{主机}} {{起始端口}}-{{结束端口}}`

- 传输文件（接收端）：
  `nc -l {{端口}} > {{文件名}}`

- 传输文件（发送端）：
  `nc {{主机}} {{端口}} < {{文件名}}`

- 保持连接模式：
  `nc -k -l {{端口}}`