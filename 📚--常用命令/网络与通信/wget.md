# wget

> 非交互式网络下载器。

- 下载文件
  ```shell
  wget http://example.com/file.zip
  ```

- 断点续传
  ```shell
  wget -c http://example.com/huge-file.iso
  ```

- 递归下载 (下载整个网站)
  ```shell
  wget -r http://example.com/
  ```
