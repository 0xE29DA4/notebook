# jobs

> 显示当前终端会话的 **后台作业**。它显示的是和你当前会话相关的进程，而不是整个系统的进程。

- 基本使用流程
  ```shell
  # 1. 使用 “&” 将一个作业放到后台执行
  # 将输出重定向防止干扰当前终端
  sleep 100 > /dev/null 2>&1 &

  # 2. 显示当前会话的后台作业
  jobs

  # 3. 将作业 1 带回前台执行
  fg %1

  # 4. 将作业 1 放到后台
  bg %1
  ```
