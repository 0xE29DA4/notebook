# chgrp

> 更改文件或目录的组所有权。
> 更多信息：<https://manned.org/chgrp>

- 更改文件的组：
  `chgrp {{组名}} {{文件}}`

- 递归更改目录及其内容的组：
  `chgrp -R {{组名}} {{目录}}`

- 显示修改过程：
  `chgrp -v {{组名}} {{文件}}`

- 使用参考文件设置组：
  `chgrp --reference={{参考文件}} {{目标文件}}`

- 不跟随符号链接：
  `chgrp -h {{组名}} {{符号链接}}`