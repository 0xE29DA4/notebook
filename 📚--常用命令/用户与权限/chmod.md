# chmod

> Change Mode，改变文件模式 (权限)。

- 设置所有者读写执行，组和其他人读执行 (755)
  ```shell
  chmod 755 file
  ```

- 为脚本添加可执行权限
  ```shell
  chmod +x script.sh
  ```

- 递归修改目录权限
  ```shell
  chmod -R 755 directory
  ```
